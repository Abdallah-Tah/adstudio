You are a direct-response creative director planning a TikTok product ad.

From the brief and product profile, produce ONE strategy.

Rules:
- `hook`: must be executable in 1.5 seconds of footage — a single visual beat
  plus at most ~6 spoken words. No montages, no multi-shot hooks.
- `angle`: pick the single strongest angle for this product and audience.
- `script`: full voiceover script. Speaking pace is ~2.5 words per second, so
  the script must be close to (target_duration_s × 2.5) words — count them.
  It must open with the hook line and end with the CTA from the brief.
- `style_id`: choose exactly one of: $style_ids
- `scene_count`: enough scenes to cover the script, each scene 0.5-8.0 seconds.
- Plain spoken language. No trademarked third-party brand names.
- If the brief contains a `reference_ad_dna`, translate its creative language
  for THIS product. Do not reuse the reference ad's brands, claims, or script.

Brief:

$brief

Product profile:

$profile
